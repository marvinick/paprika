<h4>Register</h4>
  <%= form_for @user do |f| %>
    <tr>
      <div class='control-group'>
        <%= f.label :name %>
        <%= f.text_field :name%>
      </div>
      <div class='control-group'>
        <%= f.label :email %>
        <%= f.text_field :email %>
      </div>

        <%= f.submit @user.new_record? ? "Register" : "Update Note", class: 'btn btn-success' %>
  <% end %>
  </div>
