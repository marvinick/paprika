<section id="about" class="container content-section text-center">
  <div class="row">
      <div class="col-lg-8 col-lg-offset-2">
          <h4>Congratulations, you are among the best of the best:</h4>
          <% @users.each do |user| %>
          <li>
            <h4><%= link_to user.name %></h4>
          </li>
          <% end %>
      </div>
  </div>
</section>

